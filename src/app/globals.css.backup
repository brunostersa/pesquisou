@import "tailwindcss";

/* ========================================
   🎨 SISTEMA DE CORES SIMPLIFICADO
   ========================================
   Para personalizar as cores, altere apenas as variáveis abaixo!
   ======================================== */

:root {
  /* ===== CORES PRINCIPAIS ===== */
  --primary: #3b82f6;          /* Azul principal */
  --primary-dark: #2563eb;     /* Azul escuro (hover) */
  --secondary: #8b5cf6;        /* Roxo secundário */
  --accent: #10b981;           /* Verde de destaque */
  
  /* ===== CORES DE FUNDO (MODO CLARO) ===== */
  --bg-light: #dddddd;         /* Fundo claro */
  --bg-light-secondary: #9e1f1f; /* Fundo secundário claro */
  --bg-light-tertiary: #f1f5f9;  /* Fundo terciário claro */
  --bg-card: #ffffff;          /* Fundo dos cards */
  
  /* ===== CORES DE TEXTO (MODO CLARO) ===== */
  --text-dark: #171717;        /* Texto escuro */
  --text-gray: #64748b;        /* Texto cinza */
  --text-light: #94a3b8;       /* Texto claro */
  --text-white: #ffffff;       /* Texto branco */
  
  /* ===== CORES DE BORDA (MODO CLARO) ===== */
  --border-light: #e2e8f0;     /* Borda clara */
  --border-focus: #3b82f6;     /* Borda no foco */
  
  /* ===== CORES DE ESTADO ===== */
  --success: #10b981;          /* Verde sucesso */
  --warning: #f59e0b;          /* Amarelo aviso */
  --error: #ef4444;            /* Vermelho erro */
  
  /* ===== SOMBRAS ===== */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

/* ===== MODO ESCURO (ATIVADO VIA CLASSE) ===== */
html[data-theme="dark"] {
  /* ===== FUNDOS (MODO ESCURO) ===== */
  --bg-light: #0a0a0a;           /* Fundo escuro */
  --bg-light-secondary: #1e293b; /* Fundo secundário escuro */
  --bg-light-tertiary: #334155;  /* Fundo terciário escuro */
  --bg-card: #1e293b;            /* Fundo dos cards escuro */
  
  /* ===== TEXTOS (MODO ESCURO) ===== */
  --text-dark: #ededed;          /* Texto claro */
  --text-gray: #94a3b8;          /* Texto cinza */
  --text-light: #64748b;         /* Texto suave */
  
  /* ===== BORDAS (MODO ESCURO) ===== */
  --border-light: #334155;       /* Borda escura */
  --border-focus: #60a5fa;       /* Borda no foco */
}

/* ========================================
   🎨 BASE E ELEMENTOS GERAIS
   ======================================== */

body, html, #__next {
  background-color: var(--bg-light);
  color: var(--text-dark);
  font-family: var(--font-geist-sans), -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  line-height: 1.6;
  transition: background-color 0.3s ease, color 0.3s ease;
}

/* ===== GARANTIR TEMA CLARO POR PADRÃO ===== */
html:not([data-theme="dark"]) {
  color-scheme: light;
}

html[data-theme="dark"] {
  color-scheme: dark;
}

/* ===== RESPONSIVIDADE ===== */
@media (max-width: 768px) {
  :root {
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow-md: 0 2px 4px -1px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }
}

/* ===== UTILITÁRIOS ADICIONAIS ===== */
.gradient-primary {
  background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
}

.gradient-secondary {
  background: linear-gradient(135deg, var(--secondary) 0%, var(--accent) 100%);
}

/* ===== SOMBRAS RESPONSIVAS ===== */
.shadow-custom-sm { box-shadow: var(--shadow-sm); }
.shadow-custom-md { box-shadow: var(--shadow-md); }
.shadow-custom-lg { box-shadow: var(--shadow-lg); }

/* ===== FONTES ===== */
.font-primary { font-family: var(--font-geist-sans), -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
.font-mono { font-family: var(--font-geist-mono), 'Fira Code', 'Monaco', 'Cascadia Code', monospace; }

/* ===== TAMANHOS DE FONTE ===== */
.text-base { font-size: 1rem; line-height: 1.5; }
.text-lg { font-size: 1.125rem; line-height: 1.75; }
.text-xl { font-size: 1.25rem; line-height: 1.75; }
.text-2xl { font-size: 1.5rem; line-height: 2; }
.text-3xl { font-size: 1.875rem; line-height: 2.25; }

/* ===== ESPAÇAMENTOS ===== */
.space-y-1 > * + * { margin-top: 0.25rem; }
.space-y-2 > * + * { margin-top: 0.5rem; }
.space-y-3 > * + * { margin-top: 0.75rem; }
.space-y-4 > * + * { margin-top: 1rem; }
.space-y-6 > * + * { margin-top: 1.5rem; }
.space-y-8 > * + * { margin-top: 2rem; }

.space-x-1 > * + * { margin-left: 0.25rem; }
.space-x-2 > * + * { margin-left: 0.5rem; }
.space-x-3 > * + * { margin-left: 0.75rem; }
.space-x-4 > * + * { margin-left: 1rem; }
.space-x-6 > * + * { margin-left: 1.5rem; }
.space-x-8 > * + * { margin-left: 2rem; }
